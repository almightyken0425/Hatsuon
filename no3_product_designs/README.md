# no3_product_designs/

本目錄是 **Hatsuon 落地層設計** 的容器，承載各 module 的 Module Design git。

目前無 Design git。當 Hatsuon 進入設計工件階段時，依 `~/.claude/skills/decision_framework_router/products_registry.md` 末段「新 Design git」SOP 在本目錄建立 `<module_id>/` 子目錄並 git init。
